import styled, { css } from "styled-components"


export const LabelStyles = styled.label(() => {

  return css`
    height: 20px;
    width: 70%;
    overflow: hidden;
    margin-left: 10px;
    cursor: pointer;
  `;
})
